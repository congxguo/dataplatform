apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: iceberg
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: postgres:16
        env:
        - name: POSTGRES_USER
          value: iceberg
        - name: POSTGRES_PASSWORD
          value: iceberg
        - name: POSTGRES_DB
          value: iceberg
        ports:
        - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: iceberg
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
