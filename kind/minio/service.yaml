apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: minio
spec:
  clusterIP: None
  selector:
    app: minio
  ports:
    - port: 9000
      name: s3
    - port: 9001
      name: console
